'use client';

import React from 'react';

export function AuthProvider({ children }: { children: React.ReactNode }) {
  // Placeholder auth provider - will be connected to next-auth later
  return <>{children}</>;
}
